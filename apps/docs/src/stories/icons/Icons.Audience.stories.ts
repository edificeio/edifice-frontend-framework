import { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
  title: "Design System/Icons/Audience Icons",
};

export default meta;

type Story = StoryObj;

export const Base: Story = {};
